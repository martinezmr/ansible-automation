---
- name: Gather Facts from Arista EOS Devices
  hosts: eos
  gather_facts: false

  tasks:
  - name: Gather facts using eos_facts module
    arista.eos.eos_facts:
      gather_subset: all
    register: eos_facts_result

  - name: Display gathered facts
    debug:
      var: eos_facts_result
